package org.ssk.item49;

public class AssertEx {

    private static void sort(long a[], int offset, int length){
        assert a != null;
        assert offset >= 0;
        assert length >= 0;

    }
}
